items = int(input('Введите кол-во товаров:'))

def find_shipping_cost(items):
    if items > 1:
        delivery_cost = 100 + ((items - 1) * 50)
    elif items == 1:
        delivery_cost = 100
    return delivery_cost
print(find_shipping_cost(items))
