def task5():
    a = input('Введите символ:')
    b = input('Введите символ:')
    if ord(a) < ord(b):
        first_value = ord(a)
        second_value = ord(b) + 1
    else:
        first_value = ord(b)
        second_value = ord(a) + 1
    for i in range(first_value, second_value):
        print(chr(i), end=' ')
task5()
